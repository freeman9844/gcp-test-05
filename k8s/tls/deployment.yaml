apiVersion: apps/v1
kind: Deployment
metadata:
  name: grpc-server-tls
  labels:
    app: grpc-server-tls
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: grpc-server-tls
      version: v1
  template:
    metadata:
      labels:
        app: grpc-server-tls
        version: v1
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        fsGroup: 65532
      containers:
      - name: grpc-server
        image: REGION-docker.pkg.dev/PROJECT_ID/REPOSITORY/grpc-server-tls:v1
        ports:
        - containerPort: 50051
          name: grpc
          protocol: TCP
        env:
        - name: VERSION
          value: "v1"
        - name: TLS_CERT_FILE
          value: "/etc/tls/tls.crt"
        - name: TLS_KEY_FILE
          value: "/etc/tls/tls.key"
        volumeMounts:
        - name: tls-certs
          mountPath: /etc/tls
          readOnly: true
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
        livenessProbe:
          grpc:
            port: 50051
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          grpc:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
      volumes:
      - name: tls-certs
        secret:
          secretName: grpc-tls-secret
